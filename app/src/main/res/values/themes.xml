<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Hunters" parent="Base.Theme.Hunters" />

    <!-- Base application theme. -->
    <style name="Base.Theme.Hunters" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primary</item>
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorTertiary">@color/tertiary</item>
    </style>

    <style name="ThemeOverlay.Hunters.ActionBar" parent="ThemeOverlay.Material3.ActionBar" />

    <style name="ThemeOverlay.Hunters.Popup" parent="ThemeOverlay.Material3.Light" />

</resources>